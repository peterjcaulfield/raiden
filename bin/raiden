#!/usr/bin/env node

const program = require('commander');

program
    .command('request [name]', 'execute the named request');

program
    .command('config', 'interact with .raiden configs');

program
    .command('initpath', 'return the initpath', { noHelp: true });

program
    .parse(process.argv);



