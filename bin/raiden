#!/usr/bin/env node

const program = require('commander');

program
    .command('request <request>', 'execute a http request')
    .command('config', 'interact with .raiden config files')
    .command('initpath', 'return the initpath', { noHelp: true })
    .parse(process.argv);



