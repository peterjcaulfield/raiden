#!/usr/bin/env node
var program = require('commander'),
    request = require('../lib/cmds/request');

program
    .option('-e, --env <env>', 'the environment key which specifies the hostname')
    .parse(process.argv);

request.exec(program);

