const config = {
    requests: {
        foo: 'foo request config',
        bar: 'bar request config',
        baz: 'baz request config'
    },
    envs: {
        bar: 'bar.com',
        default: 'foo.com'
    }
};

module.exports = config;
