const config = {
    requests: {
        foo: 'foo request config',
        bar: 'bar request config',
        baz: 'baz request config'
    },
    envs: {
        foo: 'foo.com',
        bar: 'bar.com',
        default: 'baz.com'
    }
};

module.exports = config;
